/*
** EPITECH PROJECT, 2019
** dante generator
** File description:
** maze_format
*/

#include "dante.h"

tile_t *maze_format(vector_t size, int perfect)
{
    tile_t *maze = maze_init(size);

    if (!perfect) {
        // maze
    } else {
        // maze
    }
    return (maze);
}